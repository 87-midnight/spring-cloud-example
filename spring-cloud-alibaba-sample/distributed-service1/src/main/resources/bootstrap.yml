server:
  port: 9012
spring:
  application:
    name: service1-provider
  cloud:
    alicloud:
      oss:
        endpoint: xxx
      access-key: xxx
      secret-key: xxx
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        prefix: common # 对应配置中心填写的data-id为common.properties。如果yml里配置了profile，则common-${profile}.properties